Program, running locally, 
    - has simple menu to: list local files 
    - allows a user to input an encryption key (4 digits)
    - allows a user to select which text files to encrypt
    - menu has a option to decrypt local files using the key
    - menu has exit option

Using local menu     
    - upload files to cloud storage and delete that (original) file locally (only encrypted files stored locally)
    - menu lists cloud files
    - gets file from cloud to local
 
Created two cloud based folders, one for text one for all other file types 
    - allows a user to list the contents of a remote folder 
    - allows a user to remove all (cloud) files that are greater in size than a user specified size
      (for example, all cloud storage files greater than 1000 bytes)
 
